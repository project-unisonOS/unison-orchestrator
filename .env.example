# Unison Orchestrator Service
# Feature flags and security controls

# Require consent grants for protected endpoints. When true, /ingest enforces
# ConsentScopes.INGEST_WRITE via consent introspection. Default is false for staged rollout.
UNISON_REQUIRE_CONSENT=false
